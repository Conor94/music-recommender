{
  "name": "music-recommender-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run build && env-cmd -f ./.env.local node ./src/index.ts",
    "dev": "npm run build && env-cmd -f ./.env.local npx nodemon ./src/index.ts --watch ./src --ext ts",
    "build": "tsc -b",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "nodemon": "^2.0.15",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.2"
  },
  "dependencies": {
    "body-parser": "^1.19.2",
    "cors": "^2.8.5",
    "env-cmd": "^10.1.0",
    "express": "^4.17.3"
  }
}
